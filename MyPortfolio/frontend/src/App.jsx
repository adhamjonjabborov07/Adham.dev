import React, { useEffect, useMemo, useState } from 'react'
import { Link, NavLink, Route, Routes, useLocation, useNavigate, useParams, useSearchParams } from 'react-router-dom'
import { Helmet } from 'react-helmet-async'
import { AnimatePresence, motion, useReducedMotion } from 'framer-motion'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
import { toast } from 'sonner'
import { ArrowDownRight, ArrowRight, Check, ChevronDown, Code2, Copy, Download, Mail, Menu, Moon, Search, Send, Sparkles, Sun, X, Zap, Layers3, MapPin, CalendarDays, CircleArrowUp, Telescope, LayoutDashboard, Braces, Laptop, MessageCircle, ChevronLeft, ChevronRight, SlidersHorizontal, Award } from 'lucide-react'
import { FaGithub, FaLinkedinIn } from 'react-icons/fa'
import { projects, categories, technologies } from './data/projects'
import { submitContactMessage } from './services/contact'

const PAGE = ' | Adham — Frontend Developer'
const year = new Date().getFullYear()

function SEO({ title = 'Adham — Frontend Developer', description = 'Frontend developer from Uzbekistan building considered, responsive digital experiences.' }) { return <Helmet><title>{title.includes('Adham') ? title : `${title}${PAGE}`}</title><meta name="description" content={description} /><meta property="og:title" content={title} /><meta property="og:description" content={description} /><meta property="og:type" content="website" /><meta name="twitter:card" content="summary_large_image" /><link rel="canonical" href={`https://adham.dev${typeof window !== 'undefined' ? window.location.pathname : ''}`} /></Helmet> }
function ScrollProgress() { const [progress, setProgress] = useState(0); useEffect(() => { const update = () => setProgress((window.scrollY / Math.max(document.documentElement.scrollHeight - window.innerHeight, 1)) * 100); update(); window.addEventListener('scroll', update, { passive: true }); return () => window.removeEventListener('scroll', update) }, []); return <div className="scroll-progress" style={{ transform: `scaleX(${progress / 100})` }} /> }
function ScrollTop() { const [show, setShow] = useState(false); useEffect(() => { const update = () => setShow(window.scrollY > 550); window.addEventListener('scroll', update, { passive: true }); return () => window.removeEventListener('scroll', update) }, []); return <button className={`scroll-top ${show ? 'visible' : ''}`} aria-label="Back to top" onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}><CircleArrowUp size={19} /></button> }
function ThemeToggle({ theme, setTheme }) { const dark = theme === 'dark'; return <button className="theme-toggle" onClick={() => setTheme(dark ? 'light' : 'dark')} aria-label={`Switch to ${dark ? 'light' : 'dark'} theme`}>{dark ? <Sun size={17} /> : <Moon size={17} />}</button> }
const navLinks = [['/', 'Home'], ['/about', 'About'], ['/projects', 'Projects'], ['/achievements', 'Achievements'], ['/contact', 'Contact']]
function Navbar({ theme, setTheme }) { const [open, setOpen] = useState(false); const [scrolled, setScrolled] = useState(false); const location = useLocation(); useEffect(() => { setOpen(false); window.scrollTo({ top: 0, behavior: 'auto' }) }, [location.pathname]); useEffect(() => { const update = () => setScrolled(window.scrollY > 16); window.addEventListener('scroll', update, { passive: true }); return () => window.removeEventListener('scroll', update) }, []); useEffect(() => { const escape = (event) => event.key === 'Escape' && setOpen(false); window.addEventListener('keydown', escape); document.body.style.overflow = open ? 'hidden' : ''; return () => { window.removeEventListener('keydown', escape); document.body.style.overflow = '' } }, [open]); return <header className={`navbar ${scrolled ? 'navbar-scrolled' : ''}`}><div className="nav-inner"><Link className="brand" to="/" aria-label="Adham home">ADHAM<span>.DEV</span></Link><nav className="desktop-nav" aria-label="Primary navigation">{navLinks.map(([to, label]) => <NavLink key={to} to={to} end={to === '/'}>{label}</NavLink>)}</nav><div className="nav-actions"><ThemeToggle theme={theme} setTheme={setTheme} /><a className="button button-small cv" href="/adham-cv.pdf" download><Download size={15} /> CV <span>↗</span></a><button className="menu-button" onClick={() => setOpen(true)} aria-label="Open navigation menu" aria-expanded={open}><Menu size={22} /></button></div></div><AnimatePresence>{open && <motion.div className="mobile-menu" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><div className="mobile-menu-top"><Link className="brand" to="/">ADHAM<span>.DEV</span></Link><button className="menu-button" onClick={() => setOpen(false)} aria-label="Close navigation menu"><X size={24} /></button></div><nav aria-label="Mobile navigation">{navLinks.map(([to, label], index) => <motion.div key={to} initial={{ x: -20, opacity: 0 }} animate={{ x: 0, opacity: 1 }} transition={{ delay: .07 * index }}><NavLink to={to}>{label}<ArrowRight size={21} /></NavLink></motion.div>)}</nav><div className="mobile-menu-bottom"><p>Available for thoughtful freelance work.</p><SocialLinks /></div></motion.div>}</AnimatePresence></header> }
function SocialLinks({ compact = false }) { return <div className={`social-links ${compact ? 'compact' : ''}`}><a href="https://github.com/" target="_blank" rel="noreferrer" aria-label="GitHub"><FaGithub size={compact ? 18 : 17} /></a><a href="https://linkedin.com/" target="_blank" rel="noreferrer" aria-label="LinkedIn"><FaLinkedinIn size={compact ? 18 : 17} /></a><a href="https://t.me/" target="_blank" rel="noreferrer" aria-label="Telegram"><Send size={compact ? 18 : 17} /></a><a href="mailto:hello@adham.dev" aria-label="Email Adham"><Mail size={compact ? 18 : 17} /></a></div> }
function Footer() { return <footer className="footer"><div className="container footer-grid"><div><Link className="brand" to="/">ADHAM<span>.DEV</span></Link><p>I design and build useful frontend experiences with care, curiosity, and a good cup of tea.</p></div><div><span className="footer-label">Navigation</span>{navLinks.map(([to, label]) => <Link key={to} to={to}>{label}</Link>)}</div><div><span className="footer-label">Say hello</span><a href="mailto:hello@adham.dev">hello@adham.dev</a><span className="muted">Tashkent, Uzbekistan</span><SocialLinks compact /></div></div><div className="container footer-bottom"><span>© {year} Adham. Built with React and curiosity.</span><button onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}>Back to top <ArrowUpIcon /></button></div></footer> }
function ArrowUpIcon() { return <span aria-hidden="true">↑</span> }
function Layout({ children, theme, setTheme }) { return <><a className="skip-link" href="#main">Skip to content</a><ScrollProgress /><Navbar theme={theme} setTheme={setTheme} /><main id="main">{children}</main><Footer /><ScrollTop /></> }
function Container({ children, className = '' }) { return <div className={`container ${className}`}>{children}</div> }
function Eyebrow({ children }) { return <p className="eyebrow"><span />{children}</p> }
function SectionHeading({ eyebrow, title, text, link }) { return <div className="section-heading"><div><Eyebrow>{eyebrow}</Eyebrow><h2>{title}</h2></div>{text && <p>{text}</p>}{link && <Link className="text-link" to={link.to}>{link.label}<ArrowRight size={17} /></Link>}</div> }
function Reveal({ children, className = '', delay = 0 }) { const reduce = useReducedMotion(); return <motion.div className={className} initial={reduce ? false : { opacity: 0, y: 20 }} whileInView={reduce ? {} : { opacity: 1, y: 0 }} viewport={{ once: true, amount: .15 }} transition={{ duration: .55, delay }}>{children}</motion.div> }
function Button({ to, href, children, secondary = false, onClick, type = 'button', className = '' }) { const content = <>{children}<ArrowRight size={17} /></>; const cn = `button ${secondary ? 'button-secondary' : ''} ${className}`; if (to) return <Link className={cn} to={to}>{content}</Link>; if (href) return <a className={cn} href={href}>{content}</a>; return <button type={type} className={cn} onClick={onClick}>{content}</button> }
function ProjectArt({ project, detail = false }) { const Icon = project.icon === 'brain' ? Sparkles : project.icon === 'cart' ? Layers3 : project.icon === 'film' ? Laptop : project.icon === 'users' ? MessageCircle : Code2; return <div className={`project-art art-${project.palette} ${detail ? 'art-detail' : ''}`} aria-label={`${project.title} project preview`} role="img"><div className="art-glow" /><div className="art-browser"><div className="browser-top"><i /><i /><i /><span>adham.dev/{project.slug}</span></div><div className="browser-body"><div className="art-side"><b /><b /><b /><b /></div><div className="art-screen"><div className="art-screen-row"><span /><span /></div><div className="art-feature"><Icon size={detail ? 44 : 30} /><div><b>{project.title}</b><small>Build better, with clarity.</small></div></div><div className="art-cards"><i /><i /><i /></div></div></div></div><span className="art-label">{project.category} · {project.year}</span></div> }
function ProjectCard({ project, showDescription = true }) { return <article className="project-card"><Link className="project-image-link" to={`/projects/${project.slug}`} aria-label={`View ${project.title} case study`}><ProjectArt project={project} /></Link><div className="project-card-info"><div className="project-card-meta"><span>{project.category}</span><span>{project.year}</span></div><h3><Link to={`/projects/${project.slug}`}>{project.title}</Link></h3>{showDescription && <p>{project.description}</p>}<div className="project-card-bottom"><div className="tags">{project.technologies.slice(0, 3).map((tech) => <span key={tech}>{tech}</span>)}</div><Link className="case-link" to={`/projects/${project.slug}`} aria-label={`Open ${project.title} case study`}><ArrowUpIcon /></Link></div></div></article> }
function HeroVisual() { return <motion.div className="hero-visual" initial={{ opacity: 0, scale: .96 }} animate={{ opacity: 1, scale: 1 }} transition={{ duration: .8, delay: .25 }}><div className="orbit orbit-one" /><div className="orbit orbit-two" /><div className="visual-dot dot-a" /><div className="visual-dot dot-b" /><div className="hero-code"><div className="code-bar"><span /><span /><span /><em>profile.js</em></div><pre><code><i>const</i> developer = {'{'}{`\n`}  name: <b>"Adham"</b>,{`\n`}  role: <b>"Frontend Developer"</b>,{`\n`}  focus: [<b>"React"</b>, <b>"UI/UX"</b>],{`\n`}  available: <strong>true</strong>{`\n`}{'}'};</code></pre></div><div className="floating-chip chip-one"><Sparkles size={17} /><span>Design systems</span></div><div className="floating-chip chip-two"><Zap size={16} /><span>Fast by default</span></div></motion.div> }
function Stats() { const data = [['06+', 'Projects built'], ['12+', 'Tools explored'], ['02', 'Years of learning'], ['84', 'Day learning streak']]; return <section className="stats-strip"><Container><div className="stats-grid">{data.map(([value, label]) => <Reveal key={label}><strong>{value}</strong><span>{label}</span></Reveal>)}</div></Container></section> }
function Home() { const [skillTab, setSkillTab] = useState('Frontend'); const skillData = { Frontend: ['HTML5', 'JavaScript', 'React', 'React Router'], 'Styling & UI': ['CSS3', 'Tailwind CSS', 'Figma', 'Framer Motion'], Tools: ['Git', 'GitHub', 'REST APIs', 'Vite'], Learning: ['Next.js', 'Node.js', 'PostgreSQL', 'Accessibility'] }; return <><SEO /><section className="hero"><Container className="hero-grid"><div className="hero-copy"><motion.div initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }}><Eyebrow>Available for freelance projects</Eyebrow></motion.div><motion.h1 initial={{ opacity: 0, y: 22 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .08 }}>I create digital experiences that feel <span>fast, clear,</span> and memorable.</motion.h1><motion.p className="hero-text" initial={{ opacity: 0, y: 15 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .16 }}>I’m Adham, a frontend developer and student from Uzbekistan. I turn thoughtful ideas into responsive interfaces built for real people.</motion.p><motion.div className="hero-actions" initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .22 }}><Button to="/projects">View my work</Button><Button to="/contact" secondary>Let’s talk</Button></motion.div><div className="hero-foot"><SocialLinks /><div className="tech-pills"><span>React</span><span>UI/UX</span><span>Performance</span></div></div></div><HeroVisual /></Container></section><Stats /><section className="section about-preview"><Container><div className="about-preview-grid"><Reveal><div className="portrait-panel"><div className="portrait-initial">A<span>.</span></div><p>Thoughtful details make the difference.</p></div></Reveal><div><SectionHeading eyebrow="A little about me" title={<>Building with intent, <i>not just effects.</i></>} /><Reveal><p className="large-copy">I enjoy the moment an idea starts to feel real: when structure meets personality, and every interaction has a reason to be there.</p><div className="values"><div><Code2 /><b>Clean code</b><span>Solid foundations make room for better work.</span></div><div><Sparkles /><b>Thoughtful design</b><span>Clarity is always more valuable than noise.</span></div><div><Telescope /><b>Continuous learning</b><span>Every project is a chance to improve.</span></div></div><Link className="text-link" to="/about">More about my journey <ArrowRight size={17} /></Link></Reveal></div></div></Container></section><section className="section skills-section"><Container><SectionHeading eyebrow="Toolkit" title={<>Tools I use to turn <i>ideas into interfaces.</i></>} text="I pick the simplest tool that helps the product feel clear and capable." /><div className="skills-layout"><div className="skill-tabs" role="tablist" aria-label="Skill categories">{Object.keys(skillData).map((tab) => <button key={tab} role="tab" aria-selected={skillTab === tab} className={skillTab === tab ? 'active' : ''} onClick={() => setSkillTab(tab)}>{tab}<ArrowRight size={15} /></button>)}</div><div className="skills-display">{skillData[skillTab].map((skill, index) => <motion.div key={skill} className="skill-item" initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .06 }}><span>{String(index + 1).padStart(2, '0')}</span><b>{skill}</b><em>{skillTab === 'Learning' ? 'Learning' : index < 2 ? 'Advanced' : 'Intermediate'}</em></motion.div>)}</div></div></Container></section><section className="section featured-projects"><Container><SectionHeading eyebrow="Selected work" title={<>A few things I’ve <i>enjoyed building.</i></>} link={{ to: '/projects', label: 'View all projects' }} /><div className="featured-grid">{projects.filter((project) => project.featured).slice(0, 4).map((project, index) => <Reveal key={project.slug} delay={index * .06}><ProjectCard project={project} /></Reveal>)}</div></Container></section><section className="section process-section"><Container><SectionHeading eyebrow="How I work" title={<>From a good question to a <i>useful answer.</i></>} /><div className="process-grid">{[[Search, '01', 'Discover', 'Listen closely, clarify the goal, and understand the people using it.'], [SlidersHorizontal, '02', 'Plan', 'Shape the content, structure, and technical approach before moving fast.'], [LayoutDashboard, '03', 'Design', 'Create a visual system that helps the right things stand out.'], [Braces, '04', 'Build & improve', 'Develop, test, refine, and keep asking how it can feel better.']].map(([Icon, no, title, text]) => <Reveal key={title}><div className="process-item"><span>{no}</span><Icon size={26} /><h3>{title}</h3><p>{text}</p></div></Reveal>)}</div></Container></section><section className="section timeline-section"><Container><SectionHeading eyebrow="The path so far" title={<>Still learning. Already <i>building.</i></>} /><div className="timeline">{[['2024', 'Started building for the web', 'Learned the fundamentals of HTML, CSS, and JavaScript through small, useful projects.'], ['2025', 'Moved deeper into React', 'Began building responsive applications and learned how components can turn a design into a system.'], ['2026', 'Sharpening product thinking', 'Exploring educational technology, interface design, IELTS/SAT studies, and freelance-ready work.']].map(([date, title, text]) => <Reveal key={date}><article><time>{date}</time><div><h3>{title}</h3><p>{text}</p><span>Learning in public</span></div></article></Reveal>)}</div></Container></section><ContactCTA /></> }
function ContactCTA() { const copy = async () => { await navigator.clipboard?.writeText('hello@adham.dev'); toast.success('Email copied to your clipboard.') }; return <section className="contact-cta"><Container><div><Eyebrow>Have a project in mind?</Eyebrow><h2>Let’s make something <i>worth using.</i></h2><p>I’m open to thoughtful collaborations, frontend work, and a good conversation about a useful idea.</p></div><div className="cta-actions"><Button to="/contact">Start a conversation</Button><button className="copy-button" onClick={copy}><Copy size={16} /> Copy email</button><span><i /> Available for new work</span></div></Container></section> }
function Projects() { const [params, setParams] = useSearchParams(); const query = params.get('q') || ''; const category = params.get('category') || 'All'; const tech = params.get('tech') || 'All'; const featured = params.get('featured') === 'true'; const sort = params.get('sort') || 'newest'; const update = (name, value) => { const next = new URLSearchParams(params); value === 'All' || value === '' || value === false ? next.delete(name) : next.set(name, value); setParams(next) }; const filtered = useMemo(() => projects.filter((p) => (!query || `${p.title} ${p.description}`.toLowerCase().includes(query.toLowerCase())) && (category === 'All' || p.category === category) && (tech === 'All' || p.technologies.includes(tech)) && (!featured || p.featured)).sort((a, b) => sort === 'az' ? a.title.localeCompare(b.title) : sort === 'oldest' ? a.year - b.year : b.year - a.year), [query, category, tech, featured, sort]); return <><SEO title="Projects" description="Selected frontend, design, and learning projects by Adham." /><PageHero eyebrow="Selected projects" title={<>Useful products, <i>carefully made.</i></>} text="A growing collection of frontend work, product explorations, and projects that helped me learn by building." /><section className="section projects-page"><Container><div className="filters"><label className="search-control"><Search size={18} /><input value={query} onChange={(e) => update('q', e.target.value)} placeholder="Search projects" aria-label="Search projects" /></label><div className="filter-row"><select value={category} onChange={(e) => update('category', e.target.value)} aria-label="Filter by category">{categories.map((item) => <option key={item}>{item}</option>)}</select><select value={tech} onChange={(e) => update('tech', e.target.value)} aria-label="Filter by technology">{technologies.map((item) => <option key={item}>{item}</option>)}</select><select value={sort} onChange={(e) => update('sort', e.target.value)} aria-label="Sort projects"><option value="newest">Newest</option><option value="oldest">Oldest</option><option value="az">A–Z</option></select><button className={`featured-filter ${featured ? 'active' : ''}`} onClick={() => update('featured', !featured)}><Sparkles size={15} /> Featured</button><button className="clear-filter" onClick={() => setParams({})}>Clear</button></div></div><p className="results-count">{filtered.length} {filtered.length === 1 ? 'project' : 'projects'} found</p>{filtered.length ? <motion.div layout className="projects-grid">{filtered.map((project) => <motion.div layout key={project.slug}><ProjectCard project={project} /></motion.div>)}</motion.div> : <div className="empty-state"><Search size={28} /><h2>No matching projects</h2><p>Try another search or clear your current filters.</p><Button onClick={() => setParams({})}>Clear filters</Button></div>}</Container></section></> }
function PageHero({ eyebrow, title, text }) { return <section className="page-hero"><Container><Eyebrow>{eyebrow}</Eyebrow><h1>{title}</h1><p>{text}</p></Container></section> }
function ProjectDetails() { const { slug } = useParams(); const navigate = useNavigate(); const project = projects.find((item) => item.slug === slug); if (!project) return <NotFound project />; const index = projects.indexOf(project); const previous = projects[(index - 1 + projects.length) % projects.length]; const next = projects[(index + 1) % projects.length]; const related = projects.filter((item) => item.slug !== project.slug && item.category === project.category).slice(0, 2); return <><SEO title={project.title} description={project.description} /><section className="project-detail-hero"><Container><button className="breadcrumb" onClick={() => navigate('/projects')}><ChevronLeft size={17} /> All projects</button><div className="detail-title"><div><span>{project.category} · {project.year}</span><h1>{project.title}</h1><p>{project.description}</p></div><div className="detail-actions"><a className="button" href="#overview">Project overview <ArrowDownRight size={17} /></a><a className="icon-link" href="https://github.com" target="_blank" rel="noreferrer" aria-label="Open GitHub repository"><FaGithub size={19} /></a></div></div><ProjectArt project={project} detail /></Container></section><section className="section project-content" id="overview"><Container><aside className="project-aside"><span>Project details</span><dl><div><dt>Role</dt><dd>{project.role}</dd></div><div><dt>Timeline</dt><dd>{project.timeline}</dd></div><div><dt>Team</dt><dd>Independent project</dd></div><div><dt>Status</dt><dd><i /> {project.status}</dd></div></dl></aside><div className="case-study"><article><span>01 — Overview</span><h2>A practical idea, shaped into a clear interface.</h2><p>{project.solution} The aim was not to add more features than necessary—it was to make each important one feel obvious, calm, and responsive.</p></article><div className="case-two"><article><span>02 — The problem</span><h3>{project.problem}</h3></article><article><span>03 — The solution</span><h3>{project.solution}</h3></article></div><article className="feature-story"><span>04 — What I built</span><h2>Made for the next useful action.</h2><ul>{project.features.map((feature) => <li key={feature}><Check size={17} />{feature}</li>)}</ul></article><div className="stack-block"><span>Technology stack</span><div>{project.technologies.map((item) => <b key={item}>{item}</b>)}</div></div><article><span>05 — What I learned</span><h2>Better details create better habits.</h2><p>{project.outcomes.join('. ')}. Each project is another opportunity to make implementation more intentional—from flexible layout systems to honest interface states.</p></article></div></Container></section>{related.length > 0 && <section className="section related"><Container><SectionHeading eyebrow="More to explore" title={<>Related <i>projects.</i></>} /><div className="related-grid">{related.map((item) => <ProjectCard key={item.slug} project={item} />)}</div></Container></section>}<section className="project-switcher"><Container><Link to={`/projects/${previous.slug}`}><ChevronLeft size={21} /><span>Previous project<b>{previous.title}</b></span></Link><Link to={`/projects/${next.slug}`}><span>Next project<b>{next.title}</b></span><ChevronRight size={21} /></Link></Container></section><ContactCTA /></> }
function About() { const tools = ['React', 'JavaScript', 'CSS', 'Tailwind CSS', 'Framer Motion', 'Git', 'Figma', 'REST APIs']; return <><SEO title="About" description="About Adham, a student frontend developer from Uzbekistan." /><PageHero eyebrow="About myself" title={<>A student developer with a <i>curious mind.</i></>} text="I care about how the web feels for the person on the other side of the screen—and I enjoy making that experience better, one thoughtful detail at a time." /><section className="section about-page"><Container><div className="about-story"><div className="about-sticky"><div className="portrait-panel portrait-large"><div className="portrait-initial">A<span>.</span></div></div><p><MapPin size={16} /> Based in Uzbekistan</p></div><div><p className="large-copy">Hi, I’m Adham — a student and frontend developer from Uzbekistan. I build fast, responsive, and visually engaging web experiences that make ideas feel clear and useful.</p><p>My journey into web development began with a simple curiosity: how do the websites and apps I use every day actually work? That question led me to HTML, CSS, and JavaScript, and then to React—the tool that made me see how a good interface can become a well-organized system.</p><p>Today, I enjoy turning concepts into real products. I like planning a clear layout, refining the small interactions, and writing code that remains understandable as a project grows. I am especially interested in React, UI/UX, performance, accessibility, and educational technology because I believe technology is most valuable when it helps people learn, decide, or move forward with confidence.</p><p>Alongside development, I am preparing for IELTS and SAT. Balancing study with projects has taught me consistency, time management, and the importance of making steady progress. I do not claim to know everything yet; I am actively learning, building, and looking for opportunities that help me become a stronger developer and collaborator.</p><p>My goal is to work on meaningful digital products, collaborate with thoughtful people, and keep improving through practical work. Whether I am building a landing page, dashboard, or learning tool, I want the final result to feel focused, reliable, and pleasant to use.</p><div className="principles"><article><span>01</span><h3>Stay useful</h3><p>Good design is not decoration. It helps people understand, decide, and move forward.</p></article><article><span>02</span><h3>Keep learning</h3><p>There is always a better question to ask and a clearer way to make something.</p></article><article><span>03</span><h3>Build honestly</h3><p>I value steady progress, honest scope, and work I can confidently stand behind.</p></article></div></div></div></Container></section><section className="section learning-section"><Container><div><Eyebrow>Right now</Eyebrow><h2>Learning with purpose.</h2><p>I’m improving my frontend depth while preparing for IELTS and SAT studies. I’m drawn to challenges that make me a more useful builder and a more thoughtful collaborator.</p></div><div className="tool-cloud">{tools.map((tool) => <span key={tool}>{tool}</span>)}</div></Container></section><section className="section fun-facts"><Container><SectionHeading eyebrow="Beyond the screen" title={<>A few <i>small facts.</i></>} /><div>{[['01', 'I’m based in Uzbekistan'], ['02', 'I like learning by making'], ['03', 'Education products interest me'], ['04', 'I care about performance']].map(([no, text]) => <article key={no}><span>{no}</span>{text}<ArrowUpIcon /></article>)}</div></Container></section><ContactCTA /></> }
function Achievements() { const templates = [['Certificate', 'Course or certification title', 'Issuer / platform · Month Year'], ['Achievement', 'Competition or academic achievement', 'Organizer · Month Year'], ['Recognition', 'Award, volunteer work, or milestone', 'Organization · Month Year'], ['Certificate', 'Another learning milestone', 'Issuer / platform · Month Year']]; return <><SEO title="Achievements" description="Certificates, learning milestones, and achievements by Adham." /><PageHero eyebrow="Achievements" title={<>Learning milestones and <i>proud moments.</i></>} text="This space is ready for my certificates, awards, competition results, and the learning experiences I want to remember." /><section className="section achievements-page"><Container><div className="achievement-intro"><Award size={28} /><div><h2>My achievement wall</h2><p>These are editable templates. I’ll replace them with verified certificates and achievements as I earn them.</p></div></div><div className="achievement-grid">{templates.map(([type, title, meta], index) => <Reveal key={`${type}-${index}`} delay={index * .05}><article className="achievement-card"><div className="certificate-placeholder"><Award size={34} /><span>Certificate preview</span><b>{String(index + 1).padStart(2, '0')}</b></div><div className="achievement-card-copy"><span>{type}</span><h2>{title}</h2><p>{meta}</p><small>Replace this with your description and certificate link.</small></div></article></Reveal>)}</div><div className="achievement-empty"><Award size={24} /><div><b>More achievements coming soon.</b><p>Add a new card here whenever you complete a course, receive a certificate, or reach a milestone.</p></div></div></Container></section></> }
const contactSchema = z.object({ name: z.string().min(2, 'Please enter at least 2 characters.').max(60), email: z.string().email('Enter a valid email address.'), subject: z.string().min(3, 'Please add a short subject.').max(100), type: z.string().min(1, 'Choose a project type.'), budget: z.string().min(1, 'Choose a budget range.'), message: z.string().min(20, 'Please share a little more (20 characters minimum).').max(1200), consent: z.literal(true, { errorMap: () => ({ message: 'Please confirm before sending.' }) }) })
function Contact() { const { register, handleSubmit, formState: { errors, isSubmitting }, reset } = useForm({ resolver: zodResolver(contactSchema), defaultValues: { type: '', budget: '', consent: false } }); const submit = async (values) => { await submitContactMessage(values); toast.success(`Thanks, ${values.name}. Your message is ready to be sent!`); reset() }; const copy = async () => { await navigator.clipboard?.writeText('hello@adham.dev'); toast.success('Email copied.') }; const [faq, setFaq] = useState(null); const faqs = [['How soon can you reply?', 'Usually within one or two days.'], ['What kinds of projects are a fit?', 'Frontend builds, landing pages, product interfaces, and useful web experiments.'], ['Do you work remotely?', 'Yes. I’m based in Uzbekistan and happy to collaborate remotely.']]; return <><SEO title="Contact" description="Contact Adham about frontend and web projects." /><PageHero eyebrow="Contact" title={<>Let’s start with a <i>good hello.</i></>} text="Tell me a little about what you’re working on. I’ll read every message." /><section className="section contact-page"><Container><div className="contact-layout"><div className="contact-details"><h2>Prefer a direct line?</h2><a className="contact-email" href="mailto:hello@adham.dev">hello@adham.dev<ArrowUpIcon /></a><button className="copy-button" onClick={copy}><Copy size={16} /> Copy email</button><dl><div><dt><MapPin size={17} /> Location</dt><dd>Uzbekistan</dd></div><div><dt><CalendarDays size={17} /> Availability</dt><dd><i /> Open to new work</dd></div></dl><SocialLinks compact /></div><form className="contact-form" onSubmit={handleSubmit(submit)} noValidate><Field label="Your name" error={errors.name}><input {...register('name')} placeholder="Jane Smith" /></Field><Field label="Email address" error={errors.email}><input {...register('email')} type="email" placeholder="jane@company.com" /></Field><Field label="Subject" error={errors.subject}><input {...register('subject')} placeholder="A new project" /></Field><div className="form-split"><Field label="Project type" error={errors.type}><select {...register('type')}><option value="">Select one</option><option>Website</option><option>Web application</option><option>Design system</option><option>Other</option></select></Field><Field label="Budget" error={errors.budget}><select {...register('budget')}><option value="">Select range</option><option>Not sure yet</option><option>Under $500</option><option>$500–$1,500</option><option>$1,500+</option></select></Field></div><Field label="Tell me about it" error={errors.message}><textarea {...register('message')} placeholder="What are you hoping to build?" rows="5" /></Field><label className="checkbox-field"><input type="checkbox" {...register('consent')} /><span>I agree that Adham can use this information to respond to my inquiry.</span></label>{errors.consent && <p className="form-error">{errors.consent.message}</p>}<Button type="submit" className="submit-button">{isSubmitting ? 'Sending message…' : 'Send message'}</Button></form></div></Container></section><section className="section faq"><Container><SectionHeading eyebrow="A few answers" title={<>Frequently asked <i>questions.</i></>} /><div>{faqs.map(([question, answer], index) => <article key={question}><button onClick={() => setFaq(faq === index ? null : index)} aria-expanded={faq === index}>{question}<ChevronDown size={19} /></button><AnimatePresence>{faq === index && <motion.p initial={{ height: 0, opacity: 0 }} animate={{ height: 'auto', opacity: 1 }} exit={{ height: 0, opacity: 0 }}>{answer}</motion.p>}</AnimatePresence></article>)}</div></Container></section></> }
function Field({ label, error, children }) { return <label className="form-field"><span>{label}</span>{children}{error && <em className="form-error" role="alert">{error.message}</em>}</label> }
function NotFound({ project = false }) { return <><SEO title="Page not found" /><section className="not-found"><Container><span>404</span><h1>{project ? 'That project is not here.' : 'This page wandered off.'}</h1><p>{project ? 'The case study may have moved, or the link was not quite right.' : 'Let’s get you back to something useful.'}</p><Button to={project ? '/projects' : '/'}>{project ? 'Browse projects' : 'Back home'}</Button></Container></section></> }
function App() { const [theme, setTheme] = useState(() => localStorage.getItem('adham-theme') || (window.matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark')); useEffect(() => { document.documentElement.dataset.theme = theme; localStorage.setItem('adham-theme', theme) }, [theme]); return <Layout theme={theme} setTheme={setTheme}><Routes><Route path="/" element={<Home />} /><Route path="/projects" element={<Projects />} /><Route path="/projects/:slug" element={<ProjectDetails />} /><Route path="/about" element={<About />} /><Route path="/achievements" element={<Achievements />} /><Route path="/contact" element={<Contact />} /><Route path="*" element={<NotFound />} /></Routes></Layout> }
export default App
